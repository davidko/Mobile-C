#include <stdio.h>
#include <math.h>
int main()
{
    char* str;
    MCAgent_t agent;
    printf("Hello World!\n");
    while(1) {
      printf("This is %s from the agency at local_port 5050.\n", mc_agent_name);
      sleep(1);
    }
    return 0;
}
